// Declare modules
// mod miscellaneous;
// mod sorting;
// mod arrays;
mod core;

// use miscellaneous::almost_increasing_sequence;

// use crate::core::add_two_digits::add_to_digits;

fn main() {

  println!("{}", 10 / 3);

  // let v = vec![1, 3, 2, 1];
  // println!("{}", almost_increasing_sequence::almost_increasing_sequence(v));
}
